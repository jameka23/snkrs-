@model sneakers.Models.SneakersViewModels.MyProfileViewModel

@{
    ViewData["Title"] = "My Profile";
}

<h1 class="profileHeader">My Profile</h1>

@{var photoPath = "~/images/" + (Model.CurrUser.ImgPath ?? "noimage.jpg");}
<div class="profile">
    <div class="userProfile card text-center">
        <div class="card-header blackBack font-weight-bold">@Model.CurrUser.FirstName @Model.CurrUser.LastName</div>
        <div class="card-body blackBack">
            <div class="card blackBack">
                <div class="card-body blackBack">
                    @if (Model.CurrUser.ImgPath != null)
                    {
                        <img class="card-img-top imageThumbnail" src="~/images/@Model.CurrUser.ImgPath"
                             asp-append-version="true" />
                    }
                    <div class="card-header blackBack ">Rating: @Model.CurrUser.Rating</div>
                </div>
                <a class="btn btn-outline-success userRating" asp-action="EditProfile">Edit</a>
            </div>
        </div>
    </div>

    <div class="reviewsProfile card text-center">
        <div class="card-header blackBack font-weight-bold">Reviews About Me</div>
        <div class="card-body blackBack">
            @foreach (var item in Model.UserReviews)
            {
                <div class="card">
                    <div class="card-header " style="color:black">@item.ReviewerName said:</div>
                    <div class="card-body blackBack">
                        <div class="card-text blackBack">@item.ReviewMessage</div>
                        <div class="card-text blackBack">Rating: @item.Rating</div>
                    </div>
                </div>
            }
        </div>
    </div>

    <div class="sneakersProfile card text-center">
        <div class="card-header blackBack font-weight-bold">My Sneakers</div>
        @foreach (var item in Model.UserSneakers)
        {
            <div class="card-body blackBack">
                <div class="card blackBack">
                    <div class="card-header blackBack">@item.Title</div>
                    <div class="card-body blackBack">
                        <img class="card-img-top imageThumbnail" src="~/images/@item.ImgPath"
                             asp-append-version="true" />
                    </div>
                </div>
            </div>
        }
    </div>
</div>